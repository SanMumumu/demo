{
    // 使用 IntelliSense 了解相关属性。 
    // 悬停以查看现有属性的描述。
    // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python 调试程序: 当前文件",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": [
                // Train autoencoder
                // "--config", "configs/run/train/ae_city_rgb.yaml",
                // "--output", "debug"

                // // Train a single modality diffusion model
                "--config", "configs/run/train/ddpm_city_rgb.yaml",
                "--output", "debug",

                // // Train a Multi-modal diffusion model (SyncVP).
                // "--config", "configs/run/train/sync_city.yaml"
            ],
            "env": {
                "CUDA_VISIBLE_DEVICES": "0"
            }
        }
    ]
}